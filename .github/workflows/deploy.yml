name: Deployment
on:
  push:
    branches:
      - main
      - dev
jobs:
  test:
    env:
      MONGODB_USERNAME: veleski1kristijan
      MONGODB_PASSWORD: yHWKgsur2iw6gvZV
      MONGODB_DB_NAME: name
      MONGODB_CLUSTER_ADDRESS: cluster0.0ydjj.mongodb.net
      PORT: 8080
    environment: testing
    runs-on: ubuntu-latest
    steps:
      - name: Get Code
        uses: actions/checkout@v3
      - name: Cache dependencies
        uses: actions/cache@v3
        with:
          path: ~/.npm
          key: npm-deps-${{ hashFiles('**/package-lock.json') }}
      - name: Install dependencies
        run: npm ci
      - name: Run server
        run: npm start & npx wait-on http://127.0.0.1:$PORT
      - name: Run tests
        run: npm test
      - name: Output information
        run: |
          echo "MONGODBNAME : $MONGODB_DB_NAME"
  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Output information
        run: |
          echo "MONGODBNAME : $MONGODB_DB_NAME"
